from tkinter import *

window = Tk()
window.title("첼시탈출하기")

photo = PhotoImage(file="루카쿠.gif")

label = Label(window, image=photo)
button = Button(window, text="언해피", fg="blue", command=quit, width=30, height=3)

label.pack()
button.pack()

window.mainloop()
